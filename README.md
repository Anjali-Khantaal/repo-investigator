# repo-investigator

It is a codebase investigation agent with a web app.

A user picks a repo and asks questions like:
1. Where is authentication initialized?
2. Draft a safe change plan with evidence.
3. Trace the request flow from API entrypoint to DB write.
